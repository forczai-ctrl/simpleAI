# Optional production AI dependencies.
# Install these when wiring the deterministic agent interfaces to live LLM,
# retrieval, document parsing, and OCR services.
langgraph>=0.2.0
langchain>=0.2.0
langchain-community>=0.2.0
chromadb>=0.5.0
pymupdf>=1.24.0
pypdf>=4.3.0

